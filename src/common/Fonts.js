const fontName = 'Inter';

const fontFam = {
                                    // font weight
  black:        `${fontName}-Black`, //900
  bold:         `${fontName}-Bold`, //700
  extra_bold:   `${fontName}-ExtraBold`, //800
  extra_light:  `${fontName}-ExtraLight`, //200
  light:        `${fontName}-Light`, //300
  medium:       `${fontName}-Medium`, //500
  regular:      `${fontName}-Regular`, //400
  semi_bold:    `${fontName}-SemiBold`, //600
  thin:         `${fontName}-Thin`, //100
};

export default fontFam;
